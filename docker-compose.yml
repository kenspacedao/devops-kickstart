services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - REDIS_URL=redis://cache:6379
      - PORT=3000
    depends_on:
      - cache

  cache:
    image: redis:alpine
    ports:
      - "6379:6379"
